// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = "postgresql://neondb_owner:npg_Z8lDor4fIwOc@ep-patient-hill-a8es974n-pooler.eastus2.azure.neon.tech/neondb?sslmode=require"
}

model User {
  id       Int      @id @default(autoincrement())
  username String   @unique
  password String
  name     String
}

model Todo {
  id          Int     @id @default(autoincrement())
  userId      Int
  title       String
  description String?
  done        Boolean @default(false)
}
